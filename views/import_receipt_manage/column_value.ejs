<% _.each(data, function (obj) { %>
  <tr>

    <td><%= obj.receiptid %></td>
    <td><%= obj.time %></td>
    <td><%= obj.note %></td>

    <td>
      <button type="button" id="btnDetail<%= obj.id %>" onClick="detail(<%= obj.id %>)" class="btn btn-primary update">Detail</button> 
    </td>

  </tr>
<% }) %>