<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">Quản lý sản phẩm</h1>
    </div>
    <!-- /.col-lg-12 -->
</div>
<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                Danh sách sản phẩm
            </div>
            <!-- /.panel-heading -->
            <div class="panel-body">
                <div class="table-responsive">
                    <table class="table table-striped table-bordered table-hover" id="list-item">
                        <thead>
                          <tr>
                              <th>Mã</th>
                              <th>Loại Nguyên Liệu</th>
                              <th>Mô tả</th>
                              <th>Trạng thái</th>
                              <th>&nbsp;</th>
                          </tr>
                        </thead>
                        <tbody>
                          <% _.each(data, function (obj) { %>
                            <tr>
                              <td><%= obj.id %></td>
                              <td><%= obj.name %></td>

                              <% if (obj.description == undefined) { %>
                                <td></td>
                              <% } else { %>
                                <td><%= obj.description %></td>
                              <% } %>
                              
                              <% if (obj.deleted == true) { %>
                                <td>Đã xóa</td>
                              <% } else { %>
                                <td>Kích hoạt</td>
                              <% } %> 

                              <td>
                                <button type="button" class="btn btn-primary update">Cập nhật</button> 
                                <button type="button" class="btn btn-danger remove">Xóa</button>
                              </td>                            
                            </tr>
                          <% }) %>
                        </tbody>
                    </table>
                </div>
                <!-- /.table-responsive -->
                <div class="text-center">
                    <button type="submit" id="btnAddIngredientCategory" onClick="addIngredientCategory()" class="btn btn-lg btn-primary add">Thêm Loại Nguyên Liệu</button>
                </div>
            </div>
            <!-- /.panel-body -->
        </div>
        <!-- /.panel -->

        <div id="form" class="modal fade">
          <div class="modal-dialog">
            <div class="modal-content">
              <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
                <h4 class="modal-title">Thêm sản phẩm</h4>
              </div>
              <div class="modal-body">
                <div id="message" class="alert alert-danger" role="alert" style="display: none;"></div>
                <form role="form">
                  <div class="form-group">
                    <label for="ingredientcategory-name">Loại sản phẩm</label>
                    <input type="hidden" class="form-control" id="hd-action-type">
                    <input type="hidden" class="form-control" id="hd-ingredientcategory-id">
                    <input type="text" class="form-control" id="ingredientcategory-name" placeholder="Tên sản phẩm">
                  </div>
                  <div class="form-group">
                    <label for="ingredientcategory-description">Mô tả</label>
                    <input type="text" class="form-control" id="ingredientcategory-description" placeholder="Mô tả">
                  </div>
                  <div class="form-group">
                      <label for="ingredientcategory-status">Trạng thái </label>
                      <label class="radio-inline">
                          <input type="radio" name="ingredientcategory-status" id="rdoStatus1" value="true">Xóa
                      </label>
                      <label class="radio-inline">
                          <input type="radio" name="ingredientcategory-status" id="rdoStatus2" value="false">Kích hoạt
                      </label>
                  </div>
                </form>
              </div>
              <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">Thoát</button>
                <button type="submit" onClick="save()" class="btn btn-primary">Lưu thông tin</button>
              </div>
            </div><!-- /.modal-content -->
          </div><!-- /.modal-dialog -->
        </div><!-- /.modal -->

    </div>
    <!-- /.col-lg-12 -->
</div>
<script type="text/javascript" charset="utf-8">
    $(document).ready(function() {
        $('#list-item').DataTable();

        //Load Product Category
        // $.get( '/services/model?from=ingredientCategory&where={"deleted": false}', function( data ) {
        //   var categories = data.ingredientcategory;
        //   $.each( categories, function( key, value ) {
        //     //Key: is index
        //     // console.log("key: "+key);
        //     //Value: is obj ingredientcategory
        //     //Ex: 
        //       // {
        //       //     "id": 1,
        //       //     "name": "Sinh tố",
        //       //     "description": "Sinh tố trái cây",
        //       //     "deleted": null,
        //       //     "createdAt": "2015-01-09T08:14:55.000Z",
        //       //     "updatedAt": "2015-01-09T08:14:55.000Z"
        //       // }
        //     // console.log("value: "+value);
        //     $('#product-ingredientcategory').append('<option value='+value.id+'>'+value.name+'</option>');
        //   });
        // });
    });

    function addIngredientCategory()
    {
      $('#message').hide();
      $('#form').modal();
      $('#hd-action-type').val("1");
    }

    function updateIngredientCategory () {
      $('#message').hide();
      $('#form').modal();
      $('#hd-action-type').val("0");
    }

    function save () {
      var type = $('#hd-action-type').val();
      var name = $('#ingredientcategory-name').val();
      var description = $('#ingredientcategory-description').val();
      var status = $('input[name=ingredientcategory-status]:checked').val();

      //If type == 0 -> action is update
      //Else action is create
      var action = (type == 0) ? "update" : "create"
      var obj = {
                  "name": name, 
                  "description": description,
                  "deleted": status
                };
      var data = {
                    "from": "ingredientCategory",
                    "action": "create",
                    "createdata": JSON.stringify(obj)
                };
      $.post("/services/model", data)
        .done(function(data) 
        {
          if(data.status == 1)
          {
            ingredientcategory = data.ingredientcategory;
            //Get datatable
            var listItem = $('#list-item').DataTable();
            //Add new row to datatable
            listItem.row.add( [
                                ingredientcategory.id,
                                ingredientcategory.name,
                                (ingredientcategory.description == undefined) ? "" : ingredientcategory.description,
                                (ingredientcategory.deleted == false) ? "Kích hoạt" : "Đã xóa",
                                "<button type='button' class='btn btn-primary update'>Cập nhật</button><button type='button' class='btn btn-danger remove'>Xóa</button>"
                              ] ).draw(false);
            //.draw(false) to prevent datatable reload to page 1
            //Hiện tại vẫn còn 1 lỗi nhỏ: khi qua trang mới thì datatable ko tự chuyển trang được (sẽ fix sau)
            $('#form').modal('hide');
            resetForm();
          }
          else
          {
            $('#message').text(data.message);
            $('#message').show();
          }
        })
        .fail(function(error)
        {
          $('#message').text('Không thể cập nhật loại nguyên liệu');
          $('#message').show();
        });
    }

    function resetForm()
    {
      $('#ingredientcategory-name').val("");
      $('#ingredientcategory-description').val("");
      $('input[name=ingredientcategory-status]').attr("checked", false);
    }
</script>